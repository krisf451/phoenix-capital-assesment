const signup = (req, res) => {
  res.json({ message: "signup route working" });
};
const signin = (req, res) => {
  res.json({ message: "signup route working" });
};

module.exports = {
  signin,
  signup,
};
